#[derive(Default)]
pub struct SortedVec<T>
where
  T: PartialOrd,
{
  data: Vec<T>,
}

impl<T> SortedVec<T>
where
  T: PartialOrd,
{
  fn push(&mut self, item: T) {
    self.data.push(item);
  }

  fn len(&self) -> usize {
    self.data.len()
  }
}
